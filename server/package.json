{
  "name": "exo-express",
  "version": "1.0.0",
  "description": "Exo express",
  "main": "server.js",
  "scripts": {
    "start": "concurrently \"tsc --watch --project tsconfig.json\" \"npm run startjs\"",
    "startjs": "nodemon --experimental-specifier-resolution=node ./dist/server.js",
    "build": "tsc --project tsconfig.json",
    "server": "nodemon --experimental-specifier-resolution=node server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jeff",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.13",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "express-validator": "^6.14.0",
    "mongoose": "^6.2.4",
    "ts-node": "^10.7.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.21"
  }
}
